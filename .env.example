# Contact form delivery (see README "Before you go live").
# Without these the /api/contact endpoint returns 503 and the form falls back
# to a prefilled mailto link, so enquiries are never silently dropped.

RESEND_API_KEY=
CONTACT_FROM_EMAIL=website@horizongrowth.us
CONTACT_TO_EMAIL=info@horizongrowth.us
